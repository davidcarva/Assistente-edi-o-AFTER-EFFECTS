# Transcrição local com GPU (RTX 3060 / CUDA 12)
faster-whisper>=1.1.0

# Runtimes CUDA via pip (evita instalar CUDA toolkit completo no Windows)
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-runtime-cu12

# Captura de comando por voz (microfone)
sounddevice
soundfile

# Análise de ritmo de corte de vídeo de referência
scenedetect[opencv]

# B-roll: busca web (Openverse) e processamento de imagem
requests
pillow

# Geração de imagem por IA local (Stable Diffusion na GPU)
torch
torchvision
diffusers
transformers
accelerate
safetensors

# Seleção de tópicos/beats e emoção por IA (texto)
openai
anthropic

# Análise de vídeo de referência (estilo/perfil)
google-genai

# Voz / TTS para canais faceless
elevenlabs

# Interface gráfica (tema escuro moderno)
customtkinter
